sudo: required

language: node_js

services:
  - docker

node_js:
  - "8"

cache: yarn

env:
  global:
  - PROJECT="self"
  - ROLE="App"
  - PACKAGE_VERSION="v1.0.${TRAVIS_BUILD_NUMBER}"

script:
  - yarn release
  - docker build -t $TAG .
before_deploy:
  - docker login -u="nvlih" -p="chunchun19"
  - docker tag $(docker images $TAG -q)
  - docker image ls

