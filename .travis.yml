sudo: required

language: node_js

services:
  - docker

node_js:
  - "8"

cache: yarn

env:
  global:
  - PROJECT="self"
  - ROLE="App"
  - PACKAGE_VERSION="v1.0.${TRAVIS_BUILD_NUMBER}"

script:
  - docker build -t selfz .
before_deploy:
  - docker login -u="nvlih" -p="chunchun19"
  - docker tag $(docker images selfz -q)
  - docker image ls

